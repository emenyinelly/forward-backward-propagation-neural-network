# forward-backward-propagation-neural-network

Objective:

To apply the key concepts of forward and backward propagation by training a simple neural network and explaining how it learns from data.

Dataset Used:

Iris Dataset (Binary Classification)

For simplicity, only two classes are used.
